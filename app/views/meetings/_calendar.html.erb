<!-- Calling calendar gem -->
<%= month_calendar events: meetings do |date, meetings| %>
  <%= date %>
  <!-- looping through meetings -->
  <% meetings.each do |meeting| %>
    <div>
      <!-- Include meeting and link to meeting -->
      <%= link_to meeting.name, meeting, class: "link" %>
    </div>
  <% end %>
<% end %>